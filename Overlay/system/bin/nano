#!/system/bin/sh
# nano: wrapper to set up and run nano from terminal
# osm0sis @ xda-developers

if [ "$1" == "--term" ]; then
  term=$2;
  shift 2;
else
  term=xterm;
fi;

clear;
TERMINFO=/system/etc/terminfo TERM=$term /system/bin/nano.bin $*;
